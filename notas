<AppButton           
          action={()=> console.log('click')}
          iconName="home"
          iconColor="#fff"
          title="Home"
          bgColor="rgba(220,100,20,0.7)"
        />

//generar claves para app facebook
keytool -exportcert -alias androiddebugkey -keystore ~/.android/debug.keystore | openssl sha1 -binary | openssl base64